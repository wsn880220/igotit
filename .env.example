# 前端环境变量（Zeabur 部署时设置）
# 后端 API 地址（Zeabur 会自动注入）
VITE_API_URL=

# 后端环境变量
# 代理服务 (可选，用于绕过 YouTube 封锁)
# 格式: http://user:pass@host:port
PROXY_URL=

# Google Translation API Key (可选)
ZHIPU_AI_API_KEY=your-api-key-here

# 服务器端口
PORT=3000

# Node 环境
NODE_ENV=production

# 管理员密钥（用于 Cookies 管理 API）
# 生成方法: openssl rand -hex 32
ADMIN_SECRET_KEY=

# YouTube Cookies（Base64 编码，可选）
# 生成方法: base64 -i cookies.txt | tr -d '\n'
YOUTUBE_COOKIES_BASE64=
